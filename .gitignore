# =================================================================
#                         DEPENDENCIES
# =================================================================
# These folders are installed by running `npm install` and are very large.
/node_modules
/frontend/node_modules
/backend/node_modules


# =================================================================
#                      BUILD OUTPUT FOLDERS
# =================================================================
# This folder is generated when you run `npm run build` in the frontend.
/frontend/build


# =================================================================
#                  ENVIRONMENT VARIABLES (SECRETS)
# =================================================================
# These files contain sensitive data like database passwords and API keys.
# NEVER commit these to a public repository.
.env
.env.local
.env.development
.env.development.local
.env.test.local
.env.production
.env.production.local


# =================================================================
#                        LOGS & DEBUG FILES
# =================================================================
# These files are generated during debugging sessions or when errors occur.
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# =================================================================
#                 OPERATING SYSTEM & EDITOR FILES
# =================================================================
# These are system-specific files that don't belong in the project.
.DS_Store
Thumbs.db
.vscode/